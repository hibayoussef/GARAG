export enum ContactType {
  VENDOR = "vendor",
  CUSTOMER = "customer",
  EMPLOYEE = "employee",
  ACCOUNT = "account",
}

export enum CustomerType {
  BUSSINESS = "Business",
  INDIVIDUAL = "Individual",
}
