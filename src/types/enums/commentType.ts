export enum CommentableType {
  BRANCH = "Branch",
  STORE = "Store",
  PRODUCT = "Product",
  ORGANIZATION = "Organization",
  ACCOUNT = "Account",
  EXPENSE = "Expense",
  JOURNAL_ENTRY = "JournalEntry",
}
